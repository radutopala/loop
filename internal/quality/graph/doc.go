// Package graph builds and caches the structural artefacts the quality
// engine reduces to metrics: the file enumeration sweep, the call graph,
// the import graph, and identifier reachability.
package graph
