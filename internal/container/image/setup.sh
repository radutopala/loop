#!/bin/bash
# Custom build-time setup — runs once during 'docker build'.
# Add your commands here (e.g. go install, npm install -g, apt-get install).
# This script executes as root in the build container.
