{
  // Claude model override for this project (e.g. "claude-sonnet-4-6")
  //"claude_model": "", // defaults to "claude-sonnet-4-6"; options: "claude-opus-4-7", "claude-opus-4-6[1m]", "claude-opus-4-6", "claude-sonnet-4-6"

  // Claude binary path override (e.g. "/usr/local/bin/claude" or "claude-code")
  //"claude_bin_path": "",

  // Authentication override for this project (overrides global auth entirely)
  //"claude_code_oauth_token": "sk-ant-project-oauth-token",
  //"anthropic_api_key": "sk-ant-project-api-key",

  // Container overrides for this project
  //"container_image": "loop-agent:latest",
  //"container_memory_mb": 512,
  //"container_cpus": 1.0,

  // Preserve MCP config JSON files after container runs (useful for debugging)
  //"keep_mcp_configs": false,

  // Browser automation override for this project
  //"browser": {
  //  "enabled": false,
  //  "chrome_image": "loop-chrome:latest"
  //},

  // Quality config override for this project. Scans run on demand (panel,
  // `loop quality scan`, or the `quality_scan` MCP tool); there is no
  // background loop.
  //"quality": {
  //  // Refuse to scan when the post-exclusion file count exceeds this. 0 = engine default (25000).
  //  //"max_files": 25000,
  //  // Additional path patterns skipped during scan (applied after built-in defaults and .gitignore).
  //  //"exclude_paths": ["./generated/**", "./vendor/**"],
  //  // Per-rule overrides. Built-in rules: no_import_cycles, signal_floor, parse_fail.
  //  // Missing entries keep the global/default value; threshold 0 keeps the default.
  //  //"rules": {
  //  //  "signal_floor":     { "enabled": true, "threshold": 6000 },
  //  //  "parse_fail":       { "enabled": true, "threshold": 0.005 },
  //  //  "no_import_cycles": { "enabled": false }
  //  //}
  //},

  // Memory config override for this project
  //"memory": {
  //  "paths": ["./docs/architecture.md", "!./docs/wip"],  // prefix with ! to exclude
  //  "max_chunk_chars": 5000,
  //  "reindex_interval_sec": 300,
  //  "embeddings": {
  //    "provider": "ollama",
  //    "model": "nomic-embed-text"
  //  }
  //},

  // Extra directories to include in the workspace (mounted into containers, passed as --add-dir to Claude)
  //"extra_dirs": ["/path/to/other/project", "/path/to/shared/lib"],

  // Files copied into containers (replaces global copy_files when set)
  // Default: ["~/.claude.json"]
  //"copy_files": ["~/.claude.json"],

  // MCP server configurations specific to this project
  // Project servers take precedence over global config servers with the same name
  //"mcp": {
  //  "servers": {
  //    "my-tool": {
  //      "command": "/path/to/binary",
  //      "args": ["--flag"],
  //      "env": {"API_KEY": "secret"}
  //    }
  //  }
  //},

  // Extra environment variables for this project's containers (merged with global; project takes precedence)
  //"envs": {},

  // Mounts for this project's containers (replaces global mounts when set)
  // Format: "host_path:container_path" or "host_path:container_path:ro"
  // Relative paths are resolved relative to the project directory
  //"mounts": [
  //  "~/.claude:~/.claude",
  //  "~/.gitconfig:~/.gitconfig:ro",
  //  "~/.ssh:~/.ssh:ro",
  //  "~/.aws:~/.aws",
  //  "/var/run/docker.sock:/var/run/docker.sock",
  //  // Named volumes for caches — persisted across container runs by Docker
  //  "loop-npmcache:~/.npm",
  //  "loop-uvcache:~/.local/share/uv",
  //  "loop-cache:~/.cache",
  //  "loop-gocache:/go"
  //],

  // Permissions override for this project (replaces global permissions when set)
  //"permissions": {
  //  "owners":  { "users": [], "roles": [] },
  //  "members": { "users": [], "roles": [] }
  //},

  // Security gates override for this project.
  // Project config can: (a) disable a gate layer for this project only,
  //                     (b) prepend rules with any decision (allow/deny/approve) — first-match-wins.
  // Project config CANNOT: loosen default_decision, re-enable a globally-disabled gate,
  //                       adjust rate limits or audit settings.
  //"gates": {
  //  //"agentgate": {
  //  //  "enabled": false,   // disable for this project only (cannot re-enable if global is false)
  //  //  // Rules prepend to global — first-match-wins, so project rules apply before baseline.
  //  //  //"command_rules": [ { "commands": ["npm"], "args_patterns": ["^publish"], "decision": "deny" } ],
  //  //  //"file_rules":    [ { "paths": ["./secret-vault/**"], "operations": ["read"], "decision": "deny" } ]
  //  //},
  //
  //  // Docker proxy override — project rules prepend to global.
  //  // Example: punch a hole in the baseline binds-deny for a specific socket.
  //  //"docker_proxy": {
  //  //  //"body_rules": [
  //  //  //  {
  //  //  //    "applies_to": "POST ^/containers/create$",
  //  //  //    "json_checks": [ { "path": "HostConfig.Binds[*]", "op": "source_path_in", "values": ["^/var/run/docker\\.sock$"] } ],
  //  //  //    "decision": "allow"
  //  //  //  }
  //  //  //],
  //  //  //"http_rules": [ { "methods": ["POST"], "paths": ["^/my/route$"], "decision": "deny" } ]
  //  //}
  //},

  // Task templates specific to this project (merged with global; project overrides by name)
  // Each template must have either "prompt" (inline) or "prompt_path" (loads from ~/.loop/templates/<path>), but not both.
  //"task_templates": [
  //  {
  //    "name": "daily-summary",
  //    "description": "Project-specific daily summary",
  //    "schedule": "0 18 * * *",
  //    "type": "cron",
  //    "prompt": "Summarize today's project activity"
  //    // Or use prompt_path to load prompt from ~/.loop/templates/<file>:
  //    //"prompt_path": "daily-summary.md"
  //  }
  //],

  // Prompt shortcuts specific to this project (merged with global; project overrides by name)
  // Each shortcut must have either "prompt" (inline) or "prompt_path" (loads from .loop/shortcuts/<path>), but not both.
  //"prompt_shortcuts": [
  //  {
  //    "name": "lint",
  //    "description": "Run linter and fix issues",
  //    "prompt": "Run make lint and fix any issues found"
  //  }
  //],

  // Workflow concurrency limits for this project (overrides global values)
  // 0 means unlimited
  //"workflow_concurrency": {
  //  "max_concurrent_runs": 5,
  //  "max_concurrent_nodes": 10
  //}
}
