# Hugo + Hextra site config for Loop documentation.
#
# Build (from this directory):
#   hugo mod get -u && hugo --gc --minify
#
# CI builds via .github/workflows/pages.yaml and publishes to GitHub Pages.

baseURL: https://radutopala.github.io/loop/
title: Loop Documentation
languageCode: en-us
enableRobotsTXT: true
enableGitInfo: true

# Apply Hextra's `docs` page type to every page so they render with the
# nav-tree sidebar on the left and the on-this-page TOC on the right.
# Without this, our flat top-level layout would fall back to the basic
# single-column page template (no sidebar).
cascade:
  - _target: {}
    type: docs

# Hextra is consumed as a Hugo module — see go.mod.
# The mount remaps the current directory as Hugo's content tree, so the
# README.md / *.md files at the root of docs/ render as site pages without
# needing to be duplicated under content/.
module:
  imports:
    - path: github.com/imfing/hextra
  mounts:
    - source: .
      target: content
      excludeFiles:
        - hugo.yaml
        - go.mod
        - go.sum
        - .gitignore
        - .hugo_build.lock
        - README.md
        - public/**
        - resources/**
        - layouts/**
        - static/**
        - i18n/**

markup:
  goldmark:
    renderer:
      unsafe: true
  highlight:
    noClasses: false
    style: github-dark

# Hide the synthetic _index.md (copied from README.md at build time) from
# the sidebar tree — the homepage is reached via the navbar/title instead.
outputs:
  home: [HTML]
  page: [HTML]
  section: [HTML]

menu:
  main:
    - identifier: docs
      name: Docs
      pageRef: /
      weight: 10
    - name: GitHub
      url: https://github.com/radutopala/loop
      weight: 20
      params:
        icon: github

params:
  navbar:
    displayTitle: false
    displayLogo: true
    logo:
      path: images/logo.svg
      dark: images/logo-dark.svg
      link: /
      width: 115
      height: 30
  page:
    width: wide
  theme:
    default: system
    displayToggle: true
  footer:
    enable: true
    displayCopyright: true
    displayPoweredBy: false
  search:
    enable: true
    type: flexsearch
    flexsearch:
      index: content
  editURL:
    enable: true
    base: https://github.com/radutopala/loop/edit/main/docs
