/** Shared Loop branding mark — generated SVG with infinity icon + "Loop" text. */
import verticalLogo from "../../assets/logo-vertical.svg";

export function LoopLogo() {
  return (
    <div style={{ display: "flex", justifyContent: "center", opacity: 0.4 }}>
      <img src={verticalLogo} alt="Loop" width={80} />
    </div>
  );
}
